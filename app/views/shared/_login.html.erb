 <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= f.error_notification %>
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      placeholder: "Email",
                      label: false,
                      input_html: { class: "input-field",  autocomplete: 'off' }  %>
          <%= f.input :password,
                      required: true,
                      # hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                      placeholder: "Password",
                      label: false,
                      input_html: { class: "input-field", autocomplete: 'off' }%>

       <%= f.button :submit, "Log in", class: "btn-SignUp btn-block" %>
        <% end %>



