<div class="edit-main">
  <%= render 'shared/navbar' %>
  <div class="edit-container">
    <div class="form-container">
      <h2>Update profile</h2>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= f.error_notification %>
        <%= f.input :first_name,
                        required: true,
                        autofocus: true,
                        placeholder: "First Name",
                        label: false  %>
            <%= f.input :last_name,
                        required: true,
                        autofocus: true,
                        placeholder: "Last Name",
                        label: false  %>
            <%= f.input :email,
                        required: true,
                        autofocus: true,
                        placeholder: "Email",
                        label: false  %>
            <%= f.input :card_number,
                        required: true,
                        autofocus: true,
                        placeholder: "Credit card details",
                        label: false  %>
            <%= f.input :password,
                        required: true,
                        hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                        placeholder: "Password",
                        label: false  %>
            <%= f.input :password_confirmation,
                        required: true,
                        placeholder: "Confirm Password",
                        label: false  %>
        <button class="btn-SignUp"><%= f.button :submit, "Update" %></button>
      <% end %>
  <div class="destory-buttons-container">
    <p><%= button_to "Cancel my account", root_path, data: { confirm: "Are you sure?" }, method: :delete, class:"cancel-my-account" %></p>

    <p><%= button_to "Log out", destroy_user_session_path, method: :delete, class:"log-out"%></p>
  </div>




    </div>
  </div>
  <%= render 'shared/footer' %>
</div>
